import styled from 'styled-components';

export const RecentPosts = styled.section`
  margin-bottom: 50px;
`;

export const SectionTitle = styled.h2`
  color: ${({ theme }) => theme.fonts.primaryFont};
  text-align: center;
  font-size: 40px;
  padding: 30px 0;
`;

export const CategoryPosts = styled.section``;

export const CategoryPostBox = styled.div`
  margin: 35px 0;
`;
